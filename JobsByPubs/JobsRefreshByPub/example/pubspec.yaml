name: jobs_refresh_load_example
description: Example app for jobs_refresh_load
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  jobs_refresh_load:
    path: ../
  jobs_runners:
    path: ../../JobsRunnersByPub

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
